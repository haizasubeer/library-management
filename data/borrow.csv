import mysql.connector
import pandas as pd

# Connect to your local MySQL database
conn = mysql.connector.connect(
    host="localhost",
    user="root",
    password="750796", # Replace with your local password
    database="library_management"     # Replace with your local db name
)

# List the tables you want to export (matching your project modules)
tables = ["books", "members", "borrow", "fines"]

for table in tables:
    try:
        query = f"SELECT * FROM borrow"
        df = pd.read_sql(query, conn)
        df.to_csv(f"data/borrow.csv", index=False)
        print(f"Exported borrow.csv successfully!")
    except Exception as e:
        print(f"Could not export borrow: {e}")

conn.close()